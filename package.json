{
    "private": true,
    "name": "fe-clean-architecture",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "test": "jest",
        "test:coverage": "jest --coverage"
    },
    "devDependencies": {
        "@types/jest": "^27.5.1",
        "jest": "^28.1.0",
        "prettier": "^2.6.2",
        "ts-jest": "^28.0.3",
        "typescript": "^4.6.4"
    }
}
